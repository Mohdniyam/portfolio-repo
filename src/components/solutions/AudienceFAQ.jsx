import { useState } from "react";
import { Plus } from "lucide-react";
import SectionHeading from "../SectionHeading";
export default function AudienceFAQ({ items }) { const [open,setOpen] = useState(0); return <section className="section-pad"><div className="site-container grid gap-12 lg:grid-cols-[.65fr_1.35fr] lg:gap-24"><SectionHeading eyebrow="FAQ" title="Useful details before we talk." /><div className="border-t border-white/10">{items.map(([q,a],i) => <div key={q} className="border-b border-white/10"><h3><button type="button" onClick={() => setOpen(open === i ? -1 : i)} aria-expanded={open === i} aria-controls={`solution-faq-${i}`} className="flex w-full items-center justify-between gap-5 py-6 text-left text-base font-medium sm:text-lg">{q}<Plus className={`shrink-0 text-accent transition ${open === i ? "rotate-45" : ""}`} size={20} /></button></h3><div id={`solution-faq-${i}`} hidden={open !== i} className="pb-6 pr-10 text-sm leading-7 text-white/45 sm:text-base sm:leading-8">{a}</div></div>)}</div></div></section>; }
