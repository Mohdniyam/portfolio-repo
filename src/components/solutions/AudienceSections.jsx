import { ArrowDown, Check, X } from "lucide-react";
import SectionHeading from "../SectionHeading";
export function FitStrip({ items }) { return <section className="border-b border-white/10" aria-label="Who this solution is for"><div className="site-container flex flex-wrap items-center gap-x-8 gap-y-3 py-6"><p className="mr-3 text-[10px] uppercase tracking-[.2em] text-accent">Built for</p>{items.map(item => <p key={item} className="text-xs text-white/45">{item}</p>)}</div></section>; }
export function AudienceProblems({ problems }) { return <section className="section-pad"><div className="site-container grid gap-12 lg:grid-cols-[.7fr_1.3fr] lg:gap-24"><SectionHeading eyebrow="Does this sound familiar?" title="Where the workflow usually breaks down." copy="Specific operational friction is often a sign that the current tools no longer match how the business works." /><ul className="grid gap-px overflow-hidden rounded-3xl border border-white/10 bg-white/10 sm:grid-cols-2">{problems.map(problem => <li key={problem} className="flex min-h-36 gap-4 bg-ink p-6 text-sm leading-7 text-white/60"><X className="mt-1 shrink-0 text-[#ff806a]" size={17} />{problem}</li>)}</ul></div></section>; }
export function AudienceSolutions({ items }) { return <section className="section-pad bg-surface"><div className="site-container"><SectionHeading eyebrow="What I can build" title="A focused system around the outcome you need." /><div className="mt-14 grid gap-px overflow-hidden rounded-3xl border border-white/10 bg-white/10 md:grid-cols-2 lg:grid-cols-3">{items.map(([title,copy],i) => <article key={title} className="min-h-64 bg-surface p-7"><p className="text-xs text-accent">0{i+1}</p><h3 className="mt-10 text-xl font-medium">{title}</h3><p className="mt-4 text-sm leading-7 text-white/40">{copy}</p></article>)}</div></div></section>; }
export function WorkflowExample({ items }) { return <section className="section-pad"><div className="site-container"><SectionHeading eyebrow="Example workflow" title="From disconnected steps to one visible process." /><ol className="mt-14 grid gap-3 sm:grid-cols-2 lg:grid-cols-6">{items.map((item,i) => <li key={item} className="relative flex min-h-36 flex-col justify-between rounded-2xl border border-white/10 bg-white/[.025] p-5"><span className="text-[10px] text-accent">0{i+1}</span><p className="text-sm font-medium">{item}</p>{i < items.length-1 && <ArrowDown className="absolute -bottom-3 left-1/2 z-10 rounded-full bg-ink p-1 text-accent lg:-right-3 lg:bottom-auto lg:left-auto lg:top-1/2 lg:-rotate-90" size={24} />}</li>)}</ol></div></section>; }
export function RelevantServices({ items }) { return <section className="border-y border-white/10"><div className="site-container grid md:grid-cols-[.65fr_1.35fr]"><p className="py-8 text-xs uppercase tracking-[.2em] text-white/30 md:border-r md:border-white/10">Relevant services</p><div className="grid grid-cols-2 lg:grid-cols-4">{items.map(item => <p key={item} className="border-l border-white/10 px-5 py-8 text-sm text-white/60 first:border-l-0 md:first:border-l">{item}</p>)}</div></div></section>; }
export function WhySolutionPartner() { return <section className="section-pad bg-surface"><div className="site-container grid gap-12 lg:grid-cols-2 lg:gap-24"><SectionHeading eyebrow="Why work with me" title="Direct engineering ownership from problem to production." copy="You work with the person shaping the architecture and building the system, with decisions grounded in the business workflow." /><ul className="grid content-start gap-x-8 sm:grid-cols-2">{["Business-first discovery", "Architecture before implementation", "Frontend, backend and integrations", "Clear, direct communication", "Maintainable production systems", "Post-launch support"].map(item => <li key={item} className="flex gap-3 border-b border-white/10 py-5 text-sm text-white/55"><Check className="shrink-0 text-accent" size={17} />{item}</li>)}</ul></div></section>; }
