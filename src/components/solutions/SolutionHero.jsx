import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import { trackEvent } from "../../utils/analytics";
export default function SolutionHero({ solution, slug }) { const workHref = solution.secondaryHref?.startsWith("#") ? `/${solution.secondaryHref}` : solution.secondaryHref; return <section className="relative border-b border-white/10 pt-[72px]"><div className="grid-texture absolute inset-0 opacity-35" /><div className="site-container relative flex min-h-[min(820px,100svh)] flex-col justify-center py-20"><p className="eyebrow">{solution.eyebrow}</p><h1 className="mt-7 max-w-6xl text-[clamp(3rem,7vw,7rem)] font-medium leading-[.95] tracking-[-.065em]">{solution.title}</h1><p className="mt-8 max-w-2xl text-base leading-8 text-white/50 sm:text-lg sm:leading-9">{solution.description}</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><a href="#contact" onClick={() => trackEvent("solution_primary_cta_click", { audience: slug })} className="button button-primary justify-center">{solution.primaryCta}<ArrowUpRight size={17} /></a>{solution.secondaryCta && <a href={workHref} onClick={() => trackEvent("solution_case_study_click", { audience: slug })} className="button button-secondary justify-center">{solution.secondaryCta}<ArrowDownRight size={17} /></a>}</div></div></section>; }
