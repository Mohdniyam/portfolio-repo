import SectionHeading from "../SectionHeading";
export default function SolutionProcess({ stages }) { return <section className="section-pad"><div className="site-container"><SectionHeading eyebrow="Engagement" title="A clear path from first conversation to launch." /><ol className="mt-14 grid gap-px overflow-hidden rounded-3xl border border-white/10 bg-white/10 md:grid-cols-4">{stages.map(([title,copy],i) => <li key={title} className="min-h-64 bg-ink p-7"><p className="text-xs text-accent">0{i+1}</p><h3 className="mt-10 text-xl font-medium">{title}</h3><p className="mt-4 text-sm leading-7 text-white/40">{copy}</p></li>)}</ol></div></section>; }
