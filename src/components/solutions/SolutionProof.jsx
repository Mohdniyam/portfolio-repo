import { ArrowUpRight, CheckCircle2 } from "lucide-react";
import { trackEvent } from "../../utils/analytics";
export default function SolutionProof({ proof, slug }) { const href = proof.href?.startsWith("#") ? `/${proof.href}` : proof.href; return <section className="section-pad"><div className="site-container"><div className={`rounded-[2rem] border p-7 sm:p-10 lg:p-14 ${proof.type === "case" ? "border-[#79d3ba]/20 bg-[#091412]" : "border-white/10 bg-white/[.025]"}`}><p className="eyebrow">{proof.eyebrow}</p><div className="mt-6 grid gap-10 lg:grid-cols-[1fr_.8fr] lg:items-end"><div><h2 className="max-w-3xl text-[clamp(2.3rem,4.5vw,4.6rem)] font-medium leading-[1.03] tracking-[-.055em]">{proof.title}</h2><p className="mt-6 max-w-2xl text-base leading-8 text-white/45">{proof.copy}</p></div><div className="lg:justify-self-end">{href ? <a href={href} onClick={() => trackEvent("solution_case_study_click", { audience: slug })} className="button button-secondary">Explore relevant work <ArrowUpRight size={16} /></a> : <p className="flex max-w-sm gap-3 text-sm leading-7 text-white/50"><CheckCircle2 className="mt-1 shrink-0 text-accent" size={17} />Capabilities are scoped honestly against your requirements before delivery begins.</p>}</div></div></div></div></section>; }
